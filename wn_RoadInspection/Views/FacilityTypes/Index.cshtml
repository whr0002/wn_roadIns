@model IEnumerable<wn_web.Models.Reclaimation.FacilityType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>@Html.DisplayNameFor(model => model.FacilityTypeName)</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FacilityTypeName }) |
            @Html.ActionLink("Details", "Details", new { id=item.FacilityTypeName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FacilityTypeName })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacilityTypeName)
        </td>
    </tr>
}

</table>
